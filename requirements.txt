fastapi>=0.110
uvicorn>=0.23
pydantic>=2.6
pandas>=2.0
numpy>=1.26
yfinance>=0.2.50
requests>=2.31
python-dotenv>=1.0
streamlit>=1.36
fastmcp>=0.2.5
matplotlib

# CrewAI + Gemini (Lab 1 — original demo on Gemini)
# Pinned to versions verified working on macOS. CrewAI >=1.12 tightened Agent.llm
# validation and rejects raw langchain chat-model objects, breaking Labs 2/3 on
# fresh installs. Update with care.
crewai==1.10.1
langchain==1.2.12
langchain-google-genai==4.2.1


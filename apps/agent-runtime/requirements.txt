# Agent Runtime — heaviest service (CrewAI + LangChain + Gemini)
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27

# CrewAI + LangChain
# Pinned to versions verified working on macOS. CrewAI >=1.12 tightened
# Agent.llm validation and rejects raw langchain chat-model objects returned
# by packages/shared-config/config.py::get_llm(), breaking Labs 2/3/4.
crewai==1.10.1
langchain==1.2.12
langchain-core==1.2.18

# Gemini LLM
langchain-google-genai==4.2.1  # Google AI Studio (local dev)
langchain-google-vertexai>=1.0 # Vertex AI (production, Workload Identity)

# GCP clients
google-cloud-pubsub>=2.21
google-cloud-firestore>=2.16

# Redis (caching + rate limiting)
redis[asyncio]>=5.0

# Langfuse (LLM observability)
# Pin to v2.x: Langfuse SDK v3+ uses the OpenTelemetry endpoint
# (/api/public/otel/v1/traces) which requires Langfuse server v3+.
# Server v3 needs ClickHouse — too heavy for local dev.
# SDK v2 uses /api/public/ingestion which works with server v2 (Postgres only).
langfuse>=2.0,<3.0

# Observability
opentelemetry-sdk>=1.24
